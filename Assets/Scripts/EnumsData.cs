namespace Enums
{
    public enum BaseState { WAITING_INPUT, SELECTION_ELEMENT }
    public enum SelectionState {NONE=0, MOVING, ROTATING, SCALING, DELETING }
    public enum HudState { ADD, CHOOSE, OPTIONS }
}